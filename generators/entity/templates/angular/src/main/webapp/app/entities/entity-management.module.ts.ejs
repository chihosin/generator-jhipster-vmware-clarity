import { NgModule, CUSTOM_ELEMENTS_SCHEMA } from '@angular/core';
import { RouterModule } from '@angular/router';

import { <%= angularXAppName %>SharedModule } from 'app/shared';
<%_ Object.keys(differentRelationships).forEach(key => {
       if (key === 'User') { _%>
import { <%= angularXAppName %>AdminModule } from 'app/admin/admin.module';
<%_ }}); _%>
import {
    <%= entityAngularName %>Service,
    <%= entityAngularName %>Component,
    <%= entityAngularName %>DetailComponent,
    <%= entityAngularName %>UpdateComponent,
    <%= entityAngularName %>DeleteDialogComponent,
    <%= entityInstance %>Route,
    <%= entityInstance %>PopupRoute,
    <%= entityAngularName %>Resolve,
    <%_ if (pagination === 'pagination' || pagination === 'pager') { _%>
    <%= entityAngularName %>ResolvePagingParams,
    <%_ } else if (pagination === 'no') {_%>
    <%= entityAngularName %>ResolveNoPagingParams,
    <%_ } _%>
} from './';

import { <%= entityAngularName %>SelectModalComponent } from './<%= entityFileName %>-select-modal.component';
<%_ for (idx in relationships) { _%>
import { <%=relationships[idx].otherEntityAngularName%>FilterComponent } from 'app/entities/<%=relationships[idx].otherEntityClientRootFolder%>/<%=relationships[idx].otherEntityFileName%>/<%=relationships[idx].otherEntityFileName%>-filter.component';
<%_ } _%>

const ENTITY_STATES = [
    ...<%= entityInstance %>Route,
    ...<%= entityInstance %>PopupRoute,
];

@NgModule({
    imports: [
        <%= angularXAppName %>SharedModule,
        <%_ Object.keys(differentRelationships).forEach(key => {
              if (key === 'User') { _%>
        <%= angularXAppName %>AdminModule,
        <%_ }}); _%>
        RouterModule.forChild(ENTITY_STATES)
    ],
    declarations: [
        <%= entityAngularName %>Component,
        <%= entityAngularName %>DetailComponent,
        <%= entityAngularName %>UpdateComponent,
        <%= entityAngularName %>DeleteDialogComponent,
        <%= entityAngularName %>SelectModalComponent,
        <%_ for (idx in relationships) { _%>
        <%=relationships[idx].otherEntityAngularName%>FilterComponent
        <%_ } _%>
    ],
    entryComponents: [
        <%= entityAngularName %>Component,
        <%= entityAngularName %>UpdateComponent,
        <%= entityAngularName %>DeleteDialogComponent,
    ],
    providers: [
        <%= entityAngularName %>Service,
        <%= entityAngularName %>Resolve,
        <%_ if (pagination === 'pagination' || pagination === 'pager') { _%>
        <%= entityAngularName %>ResolvePagingParams,
        <%_ } else if (pagination === 'no') {_%>
        <%= entityAngularName %>ResolveNoPagingParams,
        <%_ } _%>
    ],
    exports: [
        <%= entityAngularName %>SelectModalComponent,
        <%_ for (idx in relationships) { _%>
        <%=relationships[idx].otherEntityAngularName%>FilterComponent
        <%_ } _%>
    ],
    schemas: [CUSTOM_ELEMENTS_SCHEMA]
})
export class <%= angularXAppName %><%= entityAngularName %>Module {}
