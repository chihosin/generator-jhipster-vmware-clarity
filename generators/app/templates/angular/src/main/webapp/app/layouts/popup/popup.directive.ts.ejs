import { Directive, ViewContainerRef } from '@angular/core';

@Directive({
    selector: '[<%= jhiPrefixDashed %>-popup]'
})
export class PopupDirective {
    constructor(public viewContainerRef: ViewContainerRef) {
    }
}